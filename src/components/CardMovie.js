import { View, Text } from "react-native";
import React from "react";

export default function CardMovie(props) {
  const { movie } = props;
  return (
    <View>
      <Text>CardMovie</Text>
      <Image />
    </View>
  );
}
